<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.github.meixuesong.user.UserMapper" >

    <resultMap id="UserMap" type="com.github.meixuesong.user.User" >
        <id column="id" property="id" jdbcType="VARCHAR" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="phone" property="phone" jdbcType="VARCHAR" />
        <result column="address" property="address" jdbcType="VARCHAR" />
    </resultMap>

    <select id="findById" parameterType="String" resultMap="UserMap" resultType="com.github.meixuesong.user.User">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <insert id="save" parameterType="com.github.meixuesong.user.User">
      insert into user (id,name,phone,address)
      values (#{id},#{name},#{phone},#{address})
    </insert>

</mapper>
