<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.github.meixuesong.user.UserMapper">

    <resultMap id="UserMap" type="com.github.meixuesong.user.User">
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="address" property="address" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="findById" parameterType="String" resultMap="UserMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.github.meixuesong.user.User">
      insert into user (id,name,phone,address, version)
      values (#{id},#{name},#{phone},#{address}, 1)
    </insert>

    <update id="update" parameterType="com.github.meixuesong.user.User">
        update user set name=#{name},
            phone=#{phone},
            address=#{address},
            version= version + 1
        where id=#{id} and version=#{version}
    </update>

    <delete id="remove" parameterType="String">
        delete from user where id=#{id}
    </delete>


</mapper>
